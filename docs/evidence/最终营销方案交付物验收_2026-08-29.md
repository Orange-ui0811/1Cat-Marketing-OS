# 最终营销方案交付物验收（2026-08-29）

## 结论

三 Agent 固定九阶段流程已经从“完成编排”升级为“产出并人工确认完整方案”。新案例只有在
MO、PMA、BGA 候选达到正文质量门槛，MO 复盘完成，并且服务端成功汇编最终交付物后，才会
进入最后一次人工确认；短占位文本和不完整方案会明确阻塞。

## 本机实测

- Case：`case_dba04391b97d40a8ade2111663d5a460`
- 模式：`synthetic`
- 九阶段：9 / 9 completed
- Agent Run：4 / 4 `evidence_accepted`
- Commitment：4 / 4 `fulfilled`
- Handoff：2
- Approval：7（新增最终方案版本审批）
- 最终方案：`deliverable_28c352dc203441aea52d0da40bd003ff@v2`
- 方案状态：`accepted`
- 结构：10 个章节，Markdown 7,172 字符
- 发布边界：`simulated`，`external_effect=false`

机器证据位于 `.runtime/evidence/marketing-workflow-demo-synthetic-latest.json`。该目录不入库，
本文只记录不含 Secret 的验收摘要。

## 新增能力

1. Alembic `0005` 创建 `marketing_case_deliverables`，保存结构化文档、Markdown、来源、版本和人工确认状态。
2. MO 复盘成功后由 Runtime 汇编 10 章方案草稿，并建立 Case Resource 关系。
3. 最后人工门禁同时批准复盘对象和最终方案明确版本，之后 Case 才能 completed。
4. 合成模式生成完整 MO 计划、PMA 事实/Claim、Campaign 和约 5 分钟内容脚本，不再输出占位句。
5. 真实模式 Prompt 增加正文结构和最低内容要求；缺对象、正文过短、方案缺章均进入安全阻塞。
6. Workflow UI 逐章展示方案、来源和状态，并支持下载 Markdown。

## 验证结果

- Python：53 passed，2 skipped。
- 营销流程专项：9 passed。
- Workspace build：通过。
- Playwright：7 passed，包含最终方案标题、完成口径、Markdown 下载入口和刷新恢复断言。
- PostgreSQL：`0004 -> 0005` 迁移成功。
- 新合成闭环：4 Run、7 Approval、10 章节、7,172 字符，验收通过。

## 边界

完整方案仍是待人类采用和继续编辑的业务交付物。它不表示真实平台已经发布，也不表示获得真实
线索、转化或收入；真实模型输出仍必须通过同样的对象、正文和最终方案完整性检查。
